# Maintenance Guide

Run `python3 tools/validate.py` before commits. Keep raw extraction in `.work/`. Add new documents with required front matter. Use `archive-register/source-inventory.csv` for source references and avoid committing private operational records.
