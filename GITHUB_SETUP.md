# GitHub Setup

No private GitHub remote was configured during the local build.

To push later:

```bash
git remote add origin <private-repository-url>
git push -u origin main
git push origin v0.1.0
```
