# Documentation Site

MkDocs configuration lives at `mkdocs.yml`. Build later with `python3 -m mkdocs build` after installing `requirements-site.txt`.
Prototype: open `site/operating-hub-prototype.html` locally to review the single-website operating hub concept.
